name: CI Pipeline - Diff Analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize]

jobs:
  diff-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get diff
        run: |
          # Define a base para comparação (PR ou commit anterior)
          TARGET_BRANCH="${{ github.base_ref }}"
          if [ -z "$TARGET_BRANCH" ]; then
            BASE="HEAD~1"
          else
            git fetch origin $TARGET_BRANCH --depth=1
            BASE="origin/$TARGET_BRANCH"
          fi
          git diff $BASE > content.diff

      - name: Analyze diff via n8n
        run: |
          # Verifica se há conteúdo no content.diff
          if [ ! -s content.diff ]; then
            echo '{"risco": "baixo", "razao": "Nenhuma alteração para analisar", "sugestoes": ""}' > result.json
            echo "Nenhuma alteração detectada, pulando análise"
            exit 0
          fi

          echo "Enviando diff para análise via n8n..."
          RESPONSE=$(curl -s -X POST \
            -H "Content-Type: text/plain" \
            --data-binary @content.diff \
            "${{ secrets.N8N_WEBHOOK_URL }}")

          echo "$RESPONSE" > result.json
          echo "Resultado da análise:"
          cat result.json | python3 -m json.tool 2>/dev/null || cat result.json

      - name: Fail if HIGH risk
        run: |
          RISK=$(python3 -c "import json; print(json.load(open('result.json')).get('risk','UNKNOWN'))" 2>/dev/null || echo "UNKNOWN")
          echo "Risk level: $RISK"

          if [ "$RISK" = "alto" ]; then
            echo "::error::🚨 Alto risco de alteração detectada!"
            python3 -c "import json; d=json.load(open('result.json')); print(f'Razão: {d.get(\"razao\",\"N/A\")}'); print(f'Sugestões: {d.get(\"sugestoes\",\"N/A\")}')" 2>/dev/null
            exit 1
          fi

          echo "✅ Nível de risco aceitável: $RISK"